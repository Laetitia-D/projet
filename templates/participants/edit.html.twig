{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Modifier mon profil{% endblock %}

{% block body %}
    <h1>Mon profil modifier</h1>

    {{ include('participants/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('participants_index') }}">back to list</a>

    {{ include('participants/_delete_form.html.twig') }}
{% endblock %}
